# -------------------------------------------------------------------------
# BIENES RAÍCES MVC - ARCHIVO DE CONFIGURACIÓN DE EJEMPLO
# -------------------------------------------------------------------------
# Instrucciones:
# 1. Copia este archivo como '.env'
# 2. Ajusta los valores según tu entorno local.
# -------------------------------------------------------------------------

# --- Seguridad e Infraestructura (Docker) ---
# Ejecuta 'id -u' e 'id -g' en tu terminal Linux para obtener estos valores.
# Esto evita que Docker ejecute procesos como root, mapeando los archivos a tu usuario.
UID=1000
GID=1000

# --- Configuración del Administrador (Seeder/Seed) ---
# Datos para la cuenta administrativa inicial del sistema.
ADMIN_EMAIL=admin@correo.com
ADMIN_PASSWORD=admin123

# --- Base de Datos (MariaDB) ---
# DB_HOST debe coincidir con el nombre del servicio en docker-compose.yml
DB_HOST=db
DB_NAME=bienesraices_mvc
DB_ROOT_PASSWORD=root_password_seguro
DB_USER=root

# --- Configuración de Email (Entorno de Desarrollo) ---
# Por defecto se utiliza Mailpit (incluido en Docker). 
# Accede al panel web en http://localhost:8025 para ver los correos.
EMAIL_HOST=mailpit
EMAIL_PORT=1025
EMAIL_USER=null
EMAIL_PASS=null

# --- Configuración de Email (Producción / SMTP Externo) ---
# Descomenta y configura si decides usar Mailtrap o Resend.
# EMAIL_HOST=smtp.mailtrap.io
# EMAIL_PORT=2525
# EMAIL_USER=tu_usuario
# EMAIL_PASS=tu_password